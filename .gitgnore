# =========================================================
# 1. Python 编译及缓存文件
# =========================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# =========================================================
# 2. 虚拟环境 (Virtual Environments)
# =========================================================
# 强烈建议不要把虚拟环境传上去，别人应该根据 requirements.txt 自行安装
venv/
.venv/
ENV/
env.bak/
venv.bak/

# =========================================================
# 3. IDE 配置文件 (PyCharm, VS Code)
# =========================================================
# 虽然你之前上传了 .idea，但通常建议忽略它，防止不同电脑的配置冲突
.idea/
.vscode/
*.swp
*.swo
.project
.pydevproject
.settings/

# =========================================================
# 4. 打包工具输出 (PyInstaller)
# =========================================================
# 这里的 exe 和 build 文件夹不需要同步，因为它们是生成的
*.spec
dist/
build/

# =========================================================
# 5. 个人配置文件与敏感数据 (关键！)
# =========================================================
# 由于 config.json 存储了你的远程服务器 IP、用户名和 SSH 密码
# 为了安全，强烈建议不要将其上传到公共的 GitHub 仓库
config.json

# 如果你有一些本地测试用的日志或临时数据
*.log
temp/
data/

*.pyc
__pycache__/
