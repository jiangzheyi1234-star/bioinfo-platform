# H2OMeta directory-owner proof build

This private build exposes test-only descriptor seeding and fault injection.
It has a distinct distribution and module identity, is built only in required
Linux CI under `RUNNER_TEMP`, and must never be uploaded or shipped.
