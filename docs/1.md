# 大文件拆分计划 v2（基于最新代码 86b2d32）

## 现状总结

### JS 前端
- `app_galaxy.js` 仍有 **3055 行 / 124 个函数**
- 你已创建 11 个模块文件（render/, results/, services/），采用 IIFE + `global.XXX` 暴露
- **bridge services 已接入** — `bridgeToolsService`, `bridgeHistoryService`, `bridgeResultsService` 在 app_galaxy.js 中正常使用
- **render/results 模块未接入** — `HistoryPanelRenderer`, `ResultViewerRenderers`, `HistoryResultLoader` 等已写好但 app_galaxy.js 中 **21 个同名旧函数还在**，实际走的还是旧代码

### Python 后端
- `linux_settings_card.py` — 1858 行
- `database_page.py` — 1360 行
- `tool_bridge_service.py` — 1167 行
- `project_manager.py` — 1050 行（新发现，之前没提到）

---

## 第一阶段：JS 清理（app_galaxy.js 3055 → ~1200 行）

你的新模块已经写好了，只需要接入并删除旧代码。

### 1-A：接入 render 模块，删除旧渲染函数（砍 ~400 行）

在 app_galaxy.js 中，将旧函数调用替换为新模块调用，然后删除旧函数定义：

| 旧函数（删除） | 替换为 |
|---|---|
| `renderSummaryGrid(items)` | `ResultViewerRenderers.renderSummaryGrid({...})` |
| `renderArtifactList(artifacts, opts)` | `ResultViewerRenderers.renderArtifactList({...})` |
| `renderIntegratedProvenance(prov, hero)` | `ResultViewerRenderers.renderIntegratedProvenance({...})` |
| `renderIntegratedSections(sections, opts)` | `ResultViewerRenderers.renderIntegratedSections({...})` |
| `renderDatabaseResources(resources)` | `DatabasePanelRenderer.renderDatabaseResources({...})` |

**注意：** `result_shell_overrides.js` 通过 `global.renderSummaryGrid` 等覆盖渲染函数。
需要确认 overrides 是覆盖新模块暴露的接口还是旧全局函数。如果 overrides 依赖旧签名，需要同步适配。

### 1-B：接入 history 模块，删除旧历史函数（砍 ~500 行）

| 旧函数（删除） | 替换为 |
|---|---|
| `renderHistory(history)` | `HistoryPanelRenderer.renderHistoryPanel({...})` |
| `formatParamsSummary()` | HistoryPanelRenderer 内部使用 |
| `getStatusText/Class()` | HistoryPanelRenderer 内部使用 |
| `formatExactTime/RelativeTime/Duration()` | HistoryPanelRenderer 内部使用 |
| `getDurationClass()` | HistoryPanelRenderer 内部使用 |
| `filterHistoryRecords()` | `HistoryPanelRenderer.filterHistoryRecords({...})` |
| `findHistoryRecord()` | `HistoryPanelRenderer.findHistoryRecord({...})` |
| `loadExecutionResultsFromHistory()` | `HistoryResultLoader.loadExecutionResultsFromHistory({...})` |
| `openExecution()` | `HistoryResultLoader.openExecution({...})` |
| `parseHistoryParameters()` | `HistoryResultLoader.parseHistoryParameters(...)` |
| `resolveHistoryResultContext()` | `HistoryResultLoader.resolveHistoryResultContext(...)` |

### 1-C：接入 workbench selection 模块（砍 ~80 行）

| 旧函数（删除） | 替换为 |
|---|---|
| `resolveIntegratedViewSource()` | `IntegratedWorkbenchSelection.resolveIntegratedViewSource({...})` |
| `getPreferredIntegratedViewSource()` | `IntegratedWorkbenchSelection.getPreferredIntegratedViewSource({...})` |
| `getIntegratedFeatureView()` | `IntegratedWorkbenchSelection.getIntegratedFeatureView({...})` |

### 1-D：继续提取剩余可拆模块（砍 ~800 行）

app_galaxy.js 中还有以下大块可以提取到新文件：

| 新文件 | 函数 | 行数 |
|---|---|---|
| `render/run_modal.js` | openIntegratedRunModal, renderIntegratedRunModalForm, runIntegratedRunModal, closeIntegratedRunModal, ensureIntegratedRunModal, goToIntegratedRunTool | ~370 |
| `render/chart_renderer.js` | renderIntegratedChart, disposeIntegratedCharts, getDomainColor, getChartDomain, ensureEchartsLoaded, _integratedChart* 状态 | ~330 |
| `render/tool_panel.js` | loadTools, renderToolsList, createCategoryGroup, createToolItem, getCategoryName, selectTool, showToolPanel, renderInputs, renderParams, renderDatabases, browseFile, browseRemoteFile, runTool, onRunResult, clearForm 及相关辅助函数 | ~500 |
| `utils/helpers.js` | escapeHtml, setHidden, normalizePresetLabel, getRecommendedPreset, buildParamTooltipText, buildUsagePresetsPanel, getUsageGuideForParam 等 | ~200 |

### 1-E：预期结果

| 文件 | 行数 |
|---|---|
| `app_galaxy.js`（主控） | ~600-800 |
| `render/run_modal.js` | ~370 |
| `render/chart_renderer.js` | ~330 |
| `render/tool_panel.js` | ~500 |
| `render/database_panel.js` | 65（已有） |
| `render/history_panel.js` | 262（已有） |
| `render/integrated_sidebar.js` | 88（已有） |
| `render/result_viewers.js` | 134（已有） |
| `results/` 3个文件 | 513（已有） |
| `services/` 3个文件 | 183（已有） |
| `utils/helpers.js` | ~200 |

---

## 第二阶段：Python 后端拆分

### 2-A：linux_settings_card.py（1858 → ~450 行）

**步骤 1：提取 Workers → `linux_settings_workers.py`（~380 行）**

7 个 Worker 类（MiniforgeProbeWorker, MiniforgeBootstrapSubmitWorker, MiniforgePollWorker, EnvBatchCheckWorker, ToolInstallBatchPollWorker, ToolInstallSubmitWorker, RecoverInstallsWorker）+ 工具函数。

零耦合，直接搬。原文件保留 re-export：
```python
from ui.widgets.linux_settings_workers import (
    MiniforgeProbeWorker, EnvBatchCheckWorker, ...
)
```

**步骤 2：提取 Miniforge 生命周期 → `linux_settings_miniforge.py`（~300 行）**

Mixin 类，包含 probe/install/poll 整条链路。

**步骤 3：提取工具安装 → `linux_settings_tool_install.py`（~750 行）**

Mixin 类，install 提交/轮询/恢复/对话框管理。

### 2-B：database_page.py（1360 → ~550 行）

**步骤 1：提取对话框 → `database_dialogs.py`（~280 行）**

`RemoteDirectoryPickerDialog`, `DatabaseSettingsDialog`, `_AsyncTaskWorker` — 完全自包含。

**步骤 2：提取远程操作 → `database_remote_ops.py`（~200 行）**

Mixin 类：`_run_ssh`, `_list_remote_directories`, `_validate_db_root_remote`, 路径操作等。

### 2-C：tool_bridge_service.py（1167 → ~450 行）

已有 6 个卫星模块的基础上继续：

**步骤 1：提取数据类 → `tool_bridge_types.py`（~80 行）**

`ToolCheckResult`, `ExecutionResult`, `PrimerView`, `_TOOL_ARCHETYPES`。

**步骤 2：提取摘要构建 → `tool_bridge_summary_builders.py`（~250 行）**

`_build_generic_summary`, `_build_taxonomy_charts`, `_build_descriptor_driven_view_for_execution` 等。

**步骤 3：提取 PDF 生成 → `tool_bridge_report_gen.py`（~200 行）**

**步骤 4：提取远程状态 → `tool_bridge_remote_status.py`（~120 行）**

### 2-D：project_manager.py（1050 行 — 评估是否需要拆）

待分析内部结构后决定。如果有明确的功能分区（如项目 CRUD、导入导出、路径管理），可按同样模式提取 Mixin 或独立 helper。

---

## 执行顺序

| PR | 内容 | 风险 | 预计缩减 |
|---|---|---|---|
| **PR-1** | JS 清理：删除 app_galaxy.js 中 21 个重复函数，接入已有模块 | 低 | 3055 → ~2100 行 |
| **PR-2** | JS 继续拆：提取 run_modal + chart + tool_panel + utils | 中低 | 2100 → ~700 行 |
| **PR-3** | Python：linux_settings Workers 提取 + database 对话框提取 | 低 | ~660 行迁移 |
| **PR-4** | Python：Miniforge Mixin + 远程操作 Mixin + tool_bridge 类型 | 中低 | ~580 行迁移 |
| **PR-5** | Python：工具安装 Mixin + tool_bridge 摘要/报告/远程状态 | 中 | ~1120 行迁移 |

每个 PR 提交后验证：
- JS：页面加载无报错，工具/历史/结果/图表功能正常
- Python：`pytest` 全量通过
